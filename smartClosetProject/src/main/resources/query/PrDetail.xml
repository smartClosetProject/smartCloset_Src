<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.admin.prdetail.dao.PrDetailDAO">
	<!-- 상품 리스트 기본 조회 -->
		<select id="prDetailList" parameterType="prDetail" resultType="prDetail">
			select pr_num, pr_name, (pr_categoryl||' > '||pr_categorys) as category, pr_issale, pr_price
			from pr_detail order by sysdate desc
		</select>
</mapper>